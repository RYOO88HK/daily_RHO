https://github.com/isea68/DalliGo.git

1.  git clone https://github.com/isea68/DalliGo.git  :폴더 하나 만들어서 원격저장소 복제

2. git branch -r  (원격저장소의 브랜치 구경)

3. git checkout -b RHO1 : 로컬에 나의 브랜치를 만들면서 그것으로 이동.

이때  수동으로 Clone으로 받아논 DalliGo 안의 src 폴더만 내거로 덮어쓴다.

5. git status  : 변경된 거 알려준다.

4. git add .

5. git commit -m "변경내용memo"

6. git push origin RHO1 : 꼭 본인의 브랜치에 push한다음

7. 깃허브가서 pull request.  -> merger가 가능한지 문제가 있는지 나온다.

8. main으로 바로 push하지 말것. 

------------------------
##Maria DataSource
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.url=jdbc:mariadb://192.168.1.15:3306/test
spring.datasource.username=testuser
spring.datasource.password=1111


spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.url=jdbc:mariadb://127.0.0.1:8080/test
spring.datasource.username=testuser
spring.datasource.password=1111


